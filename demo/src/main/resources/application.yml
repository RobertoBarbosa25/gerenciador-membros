application.yml

spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL}
    username: ${SPRING_DATASOURCE_USERNAME}
    password: ${SPRING_DATASOURCE_PASSWORD}
  main:
    allow-bean-definition-overriding: true
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate.format_sql: true
      hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect

# Propriedades JWT
# Note a identação para app:
# 24 horas em milissegundos (86400000 ms = 24 * 60 * 60 * 1000)

server:
  port: ${PORT:8080}

# Configurações para desenvolvimento local (descomente para usar):
# spring:
#   datasource:
#     url: jdbc:postgresql://localhost:5432/gerenciador_membros
#     username: postgres
#     password: 2505
#   jpa:
#     hibernate:
#       ddl-auto: update
#     show-sql: true
# 
# server:
#   port: 8080 